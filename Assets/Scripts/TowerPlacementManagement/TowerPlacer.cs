using UnityEngine;
using System.Collections;

public class TowerPlacer : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Vector3 v = new Vector3(0f,1f,0f);
		Vector3 v1 = new Vector3(256f,256f,256f);
	//	Ray r = new Ray(Ve
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
